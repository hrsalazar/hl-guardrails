# Pinned exact: this runs unattended against a real account, so a silent dependency bump
# should not be able to change behaviour underneath it.
hyperliquid-python-sdk==0.24.0
requests==2.34.2
pandas==3.0.5
numpy==2.5.3
pyyaml==6.0.3
pywebpush==2.5.0
cryptography==50.0.1
tabulate==0.10.0
